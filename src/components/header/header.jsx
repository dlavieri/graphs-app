import React from 'react';
import './header.css';

const Header = props => {
    return (
        <header className="app-header">
            <div className="app-title">vizi graphs</div>
        </header>
    )
}

export default Header;